{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ question }}</h1>
    {#hidden comments#}
    <div>
        we will print full question here
    </div>

    <h2>Answers({{ answers|length }})</h2>
    <ul>
    {% for answer in answers %}
        <li>{{ answer }}</li>
    {% endfor %}
    </ul>
{% endblock %}